<div class="d-flex justify-content-between align-items-center mb-4">
  <h4 class="fw-bold">Manage Impact</h4>
  <% if(impact_data.length <= 0) {%>
            <a href="/impact/addform" class="btn btn-primary">Add impact</a>
    <% }%>
</div>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Count</th>
      <th>Title</th>
      <th>Content</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% if (impact_data && impact_data.length > 0) { %>
      <% impact_data.forEach((story,i) => { %>
        <tr>
          <td>
            <%= i+1 %> 
          </td>
          <td><%= story.title %></td>
          <td><%= story.content.length > 50 ? story.content.substring(0, 50) + '...' : story.content %></td>
          <td>
            <a href="/impact/editform/<%= story._id %>" class="btn btn-sm btn-warning">Edit</a>
               <form action="/impact/delete/<%= story._id %>?_method=DELETE" 
                          method="POST" 
                          class="d-inline">
                      <button class="btn btn-sm btn-danger"
                              onclick="return confirm('Delete this project?')">
                        Delete
                      </button>
                    </form>
          </td>

        </tr>
      <% }) %>
    <% } else { %>
      <tr>
        <td colspan="4" class="text-center">No impact_data found. Please add a story.</td>
      </tr>
    <% } %>
  </tbody>
</table>
